common: &default_settings
  instagram:
    proxy:
    request_delay: 0
    client_id: REPLACE_ME
    client_secret: REPLACE_ME
  web:
    username: admin
    password:
  export:
    type: stdout
  logging:
    type: stdout

test:
  <<: *default_settings

development:
  <<: *default_settings

production:
  <<: *default_settings
  web:
    username: admin
    password: admin
  export:
    type: logstash_redis
    driver: :hiredis
    url: redis://redis:6379/1
  logging:
    type: file
